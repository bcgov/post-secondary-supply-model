---
title: "Module 01 - Technical Documentation"
format:
  html:
    toc: true
    toc-location: right
    toc-depth: 3
    number-sections: true
    number-depth: 3
    theme: cerulean
    code-fold: true
    grid: 
        body-width: 1800px
        sidebar-width: 100px
        margin-width: 300px
    css: styles.css
    html-math-method: katex
    self-contained: true
---

## Overview

Script 01a executes the automated ingestion and high-fidelity preprocessing of the Student Transition Program (STP) enrolment dataset. It functions as the primary data-cleansing routine, transorming administrative records into a standardized longitudinal format. The pipeline specifically addresses temporal inconsistencies through and resolves conflicting birthdate entries across historical records.

The script further implements a multi-tiered classification logic to evaluate record validity based on student identifiers, program types, and credential categories. By filtering out developmental, skills-based, and continuing education records, the script generates a refined Record Type Table and a validated enrolment dataset. These outputs include binary flags for first-time and annual minimum enrolment sequences, providing the necessary foundational architecture for downstream transition analysis.

01b: manages the ingestion and cleansing of credential records by normalizing award dates and assigning status-based classifications to filter out non-academic or developmental achievements. It specifically synchronizes credential data with previously processed enrolment records to maintain consistency in how skills-based and developmental records are identified across the dataset.

01c: Executes a multi-dimensional ranking and temporal reconciliation of student awards to identify primary academic achievements. Through the application of a "Delay Award Effect" windowing logic, the script distinguishes between substantive hierarchical progression and concurrent secondary awards, establishing a definitive longitudinal record of educational attainment.

01d: script implements a rigorous data-cleansing framework to identify and isolate first-time enrolment events within the Student Transition Project (STP) dataset. By integrating complex identity-resolution logic and stratified proportional imputation, it resolves discrepancies in student gender and age across multiple reporting periods, ultimately generating a standardized suite of supplemental variables for downstream institutional reporting.

In most cases, identity discrepancies are handled for two distinct cohorts: those with valid Encrypted PENs and those identified by a "Student Number + PSI Code" combination. 

## Data Sources and Storage

### External Data Sources

#### [External Data Source Name]

Source: [Data provider or organization]  
Access Method: [Describe how data is accessed, e.g., API, direct download, R package]

  - [List any required credentials, API keys, or setup steps]
  - Function calls: [Relevant function or method]

Description: [Brief description of the data, coverage, and structure]

<!-- Repeat the above callout for each external data source -->

### Internal/Processed Data Sources

#### [Internal Data Product Name]

Source: [Describe how/where the data is generated or stored]  
Access Method: [Describe how to access the data]

  - File location: [Path or variable]
  - Processing methodology: [Link to documentation or code if available]

Description: [Brief description of the processed data]


### Data Storage Structure

All data is stored in a hierarchical folder structure, accessible via [describe access method/tool].

```
[BASE_FOLDER]/
├── data/
    ├── source/          # Processed intermediate files
    └── raw/             # Raw input data
        └── [subfolder]/ # [Description]
└── outputs/
    ├── tables/          # Final output tables  
    └── visuals/         # Maps and visualizations
```

## Technical Specifications

### Dependencies and Environment Requirements

#### System Requirements

- Network Access: [List required network resources]
- Storage: [Note storage requirements]
- Memory: [Note memory requirements]
- External Data: [List any required external data]

#### R Package Dependencies

The following R packages are required for this module:

- [List core data processing packages]
- [List spatial analysis packages]
- [List visualization/statistics packages]
- [List file management packages]



#### Authentication Setup

```r
# Example: API key configuration
[package]::set_api_key('YOUR_API_KEY', install = TRUE)
```

### Global Constants and Configuration

Global constants and configuration settings are centralized in a configuration file (e.g., `settings.R`). This file defines key parameters, file paths, and data validation requirements. Below is a summary of the most critical settings. For a complete list, refer to the configuration file in the project repository.

#### Data Processing and Filtering

  - `REFERENCE_YEAR`: [Year] - Reference year for primary data.
  - `CURRENT_YEAR`: [Year] - Current year for projections/analysis.
  - `FACILITY_TAG`: [Tag] - Identifier for facilities/entities.
  - `REQUIRED_COLS`: [List of required columns for validation]
  - `POP_COLS`: [List of population-related columns]


#### File Path Configuration

  - `BASE_FOLDER`: Base folder for storage.
  - `SRC_DATA_FOLDER`: Directory for source data files.
  - `RAW_DATA_FOLDER`: Directory for raw input data.
  - `OUTPUTS_FOLDER`: Directory for outputs.

[Add other relevant settings as needed]


## Methodology Overview

### STP Enrolment Data Pre-processing 

#### Source Code: (01a-enrolment-preprocessing.R)
The analytical pipeline initiates with the transformation of raw administrative records into a standardized dataset for longitudinal analysis. Initial processing involves temporal normalization via a century-prefix logic, where legacy two-digit year formats are converted into four-digit ISO 8601 standards to ensure chronological consistency across the PSI_BIRTHDATE and PSI_MIN_START_DATE fields.

Conflicting birthdates are resolved through a hierarchical tie-breaker routine to establish a consistent "single source of truth" for each student identifier. The script applies an evidentiary priority sequence:

Temporal Authority: Prioritizes the date matching the most recent "Last Seen" administrative record.

Frequency: If no temporal match is found, the date with the highest frequency of occurrence (the mode) is selected.

A multi-tiered validation logic is used to assign discrete status codes (0–8) to each record, enabling the systematic filtering of non-standard entries such as developmental or continuing education courses. Finally, the script executes a partitioned sequencing operation to identify the is_first_enrol and is_min_enrol flags, ensuring each student is represented by a logically consistent chronological record across the reporting period.

**STP Enrolment Records**

::: {.table-condensed}
| Status Code | Label | Primary Logic / Criteria |
|:---|:---|:---|
| **0** | **Good** | Default; valid enrolment records that pass all filters. |
| **1** | **Missing ID** | Records missing both PEN and Student Number. |
| **2** | **Developmental** | Records where the study level is "Developmental." |
| **3** | **No Transition** | Records flagged with an entry status of "No Transition." |
| **5** | **Outside BC** | Attending an institution outside of British Columbia. |
| **6** | **Skills Based** | Continuing Education, Audit, or specific non-credit CIP codes. |
| **7** | **Dev. CIP** | Non-skills courses falling under developmental CIP categories. |
| **8** | **Certification** | Recommendation for Certification (defined in code comments). |
:::


#### Data Dictionary

Includes variables used in this analysis (unused dataset variables are ommitted).  Calculated variables are in bold font.

##### Table: stp_enrolment/stp_enrolment_valid

<ul class="list-variable-names">
<li>ID (Integer): A unique primary key for each record in the enrolment table.</li>
<li>ENCRYPTED_TRUE_PEN (String): An encrypted version of the Provincial Education Number used for student tracking.</li>
<li>PSI_STUDENT_NUMBER (String): The unique identifier assigned to a student by the Post-Secondary Institution.</li>
<li>PSI_CODE (String): A character code representing the specific Post-Secondary Institution.</li>
<li>PSI_SCHOOL_YEAR (String): The academic year associated with the enrolment record.</li>
<li>PSI_MIN_START_DATE (Date/String): The earliest recorded start date for a student's program or course.</li>
<li>PSI_BIRTHDATE (Date/String): The raw birthdate as provided in the administrative records.</li>
<li>PSI_GENDER (String): The raw gender code provided by the reporting institution.</li>
<li>PSI_CREDENTIAL_CATEGORY (String): The classification of the credential (e.g., "Bachelors Degree").</li>
<li>PSI_CIP_CODE (String): The Classification of Instructional Programs code used to identify the field of study.</li>
<li><b>psi_birthdate_cleaned (Date)</b>: CALCULATED IN THIS ANALYSIS via hierarchical tie-breaker logic.</li>
<li><b>CONCATENATED_ID (String)</b>: CALCULATED IN THIS ANALYSIS to unify student records across institutions.</li>
</ul>

##### Table: stp_enrolment_record_type

Key variables used in this analysis, calculated varaiables are in bold font.

<ul class="list-variable-names">
<li>ID (Integer): Unique identifier used to join with the primary enrolment table.</li>
<li><b>RecordStatus (Integer)</b>: A classification code used to validate or filter records (e.g., 0 = "Good").</li>
<li><b>MinEnrolment (Integer)</b>: A binary flag (0/1) identifying the earliest recorded enrolment for a student.</li>
<li><b>FirstEnrolment (Integer)</b>: A binary flag (0/1) identifying the first chronological enrolment in the current dataset.</li>
</ul>


### STP Credential Data Pre-processing

#### Source Code: 01b-credential-preprocessing.R
The credential preprocessing pipeline begins with a temporal normalization routine, utilizing a century-prefix function to reconcile legacy two-digit year strings into four-digit ISO 8601 formats. Following standardization, the script incorporates a cross-referential validation routine that synchronizes awarded credentials with previously validated enrolment statuses via the "Skills Match" reference set.

Data integrity is maintained through a hierarchical classification system. The script adjudicates record validity by applying status codes based on the PSI_CREDENTIAL_CATEGORY and a robust exclusion logic that flags developmental CIP codes, missing identifiers, or "Recommendations for Certification." Specific program-level exceptions are applied to preserve specialized technical certificates that would otherwise meet exclusion criteria, resulting in a refined table (Status 0–8) ready for analysis.


STP Credential Records:

::: {.table-condensed}
| Status Code | Label | Primary Logic / Criteria |
|:---|:---|:---|
| **0** | **Good** | Default; valid credential records that pass all validation filters. |
| **1** | **Missing ID** | Records missing both the Encrypted PEN and the PSI Student Number. |
| **2** | **Developmental** | Credentials where the PSI level is explicitly "Developmental." |
| **6** | **Skills Based** | Matches records previously identified as "Skills Match" in enrolment data. |
| **7** | **Dev. CIP** | Falls under developmental CIPs, excluding specific approved exceptions. |
| **8** | **Certification** | Records where the category is "Recommendation for Certification." |
:::


#### Data Dictionary (Not Completed)

##### [Table 1 Name]

<ul class="list-variable-names">
<li>STQU_ID (Integer): A unique identifier for each student.</li>
<li>SURVEY_YEAR (Integer): The academic year in which the survey was administered.</li>
<li>SURVEY (String): A character code representing the survey (e.g. "BGS"). </li>
<li>INST_CD (String): A character code representing the institution in which a student was enrolled. </li>
</ul>

Key variables computed in this analysis:

<ul class="list-variable-names">
<li>Weight_NLS_CIP_NOC (Integer): RECALCULATED IN THIS ANALYSIS</li>
<li>Weight_OCC_CIP_NOC (Integer): RECALCULATED IN THIS ANALYSIS</li>
</ul>

 Variables not used in this analysis: 

<ul class="list-variable-names">
<li>AGE_AT_GRAD: NOT USED</li>
<li>Age_Grad_Group: NOT USED</li>
</ul>

### STP Credential Data Processing: (01c-credential-analysis.R)

#### Descriptive Methods

**Data Linkage and Integrity**

_Credential Supvars Enrolment Table_

Records for credentials associated with a valid (non-null) award date and whose `RecordStatus == 0` are synchronized with the most recent enrolment metadata to establish a high-fidelity baseline cohort (`credential_supvars_enrolment`) enriched with the most recent available enrolment metadata. The pipeline utilizes a two-tiered join:

* For students with a valid `ENCRYPTED_TRUE_PEN`, the script isolates the most recent enrolment record (determined by the maximum `PSI_SCHOOL_YEAR`). This cohort represents the highest-confidence matches.
* For records lacking a valid `ENCRYPTED_TRUE_PEN`, the script utilizes a composite identifier (`PSI_CODE` and `PSI_STUDENT_NUMBER`). It similarliy isolates the most recent enrolment record.

_Credential Supvars Table_

A second high-fidelity baseline cohort is created by linking credential records with foundational student metadata from STP Enrolment dataset. Validated birthdates, gender markers, and visa statuses are incorporated and formal date objects (CREDENTIAL_AWARD_DATE_D), are typecast and recoded where necessary.

Records categorized as "Developmental," "Short Certificate," or "Other/None" are tagged with a DropCredCategory = "Yes" indicator. By utilizing a left_join back to the record type table, the script preserves the full data lineage while programmatically isolating these records for targeted exclusion in final reporting.

**Missing Gender Recovery**
A hierarchical backfilling protocol resolves missing gender attributes using using the most contemporary administrative evidence available for each student with missing gender. A multi-pass recovery queries `credential_supvars_enrolment` (Pass 1) and `stp_enrolment` (Pass 2). Values are reconciled via `coalesce` based on the most recent institutional contact and joined back to the main `credential_supvars` table.  The routine prioritizes PEN-based matches, falling back to student-ID composites or the `LAST_SEEN_BIRTHDATE` administrative placeholder.

**Birthdate Recovery**
The script attempts to recover cleaned birthdates using a prioritized hierarchy: first by primary student identifier (`ENCRYPTED_TRUE_PEN`), and subsequently by institutional composite keys (`PSI_STUDENT_NUMBER` and `PSI_CODE`).  In instances where primary cleaned dates are absent, the script utilizes the `LAST_SEEN_BIRTHDATE` attribute from the STP Enrollment data sets as a final administrative authority.  

Enrichment markers—including cleaned gender, birthdates, and standardized award dates—are merged back into the core credential table.  Filters are applied to the credential table to retain only records meeting three critical integrity criteria:

- A valid administrative record status (`RecordStatus` == 0).
- Exclusion of non-standard or developmental awards (`DropCredCategory` == NA).
- Exclusion of incomplete reporting cycles (`DropPartialYear` == NA).

**Age At Graduation and Academic School Year**
Age-at-graduation and academic school year markers are derived and aligned with standard institutional reporting cycles

Student's age at the time of credential is calculated by measuring the interval between the standardized birthdate and the formal award date.  The integer age is grouped into discrete reporting cohorts, categorizing individual ages into standardized demographic groups (e.g., 18–24, 25–29).

A fiscal PSI_AWARD_SCHOOL_YEAR is derived based on the month of award issuance, ensuring consistency with the provincial academic calendar. A standard September 1st "cutoff." is used; awards issued between January and August (months 1–8) are attributed to the previous calendar year’s cycle (e.g., an award in June 2023 is grouped within the 2022/2023 school year).  Data are standardized into "YYYY/YYYY" format. 

A final gender recovery pass and deduplication routine are executed; missing values are recovered by performing a secondary lookup against the main enrolment table.  A singular, unique record with ID is isolated representing each academic achievement.

**Imputation of Missing Genders**
Stratified stochastic imputation is performed to resolve residual missing gender values. This method ensures the final dataset maintains the statistical distribution of the known population, preventing the bias inherent in simple mode substitution.  

An empirical baseline is established by calculating observed gender ratios within the population.  The credential dataset is grouped by PSI_CREDENTIAL_CATEGORY, calculating the specific frequency and corresponding probability ($n / \sum n$) of each gender marker within those cohorts.  These distributions are aggregated into a reference list of weights and categories, effectively creating a specific "probability profile" for every award type (e.g., the gender distribution for Engineering Diplomas may differ from that of Nursing Certificates).  These distributions provide required weights in a random draw, used to reconcile null entries. This "weighted coin flip" ensures the imputed data mirrors the demographic profile of the specific credential cohort.

**Rank Assignment**
The pipeline first integrates a standardized credential_rank reference table, assigning a numerical RANK to each record based on its PSI_CREDENTIAL_CATEGORY (e.g., Doctorate > Master’s > Diploma). To maintain identity resolution across the dataset, records are partitioned into two cohorts for processing:

- PEN Cohort: Students identified via a valid ENCRYPTED_TRUE_PEN.
- Student Number Cohort: Students identified via the PSI_CODE and PSI_STUDENT_NUMBER composite key.

Within each student partition, the script executes a window-function sorting routine to assign binary "Highest" flags:

- Highest Credential by Date: Determined by sorting records in descending order by CREDENTIAL_AWARD_DATE_D. In instances where multiple awards share a date, the RANK value serves as the secondary tie-breaker. The first chronological record in this sequence is flagged "Yes."
- Highest Credential by Rank: Determined by sorting records in ascending order by RANK (where lower values represent higher prestige). CREDENTIAL_AWARD_DATE_D serves as the secondary tie-breaker, prioritizing the most recent achievement within the same rank. The top-ranked record is flagged "Yes."

**Missing Age At Grad**
Missing AGE_AT_GRAD values are recovered through stratified stochastic imputation routine, leveraging empirical population distributions to populate gaps while maintaining the statistical variance of the graduate cohort.  Probability weights are derived from a reference set of students flagged as HIGHEST_CRED_BY_DATE == "Yes". Age distributions are stratified by Credential Category and Gender, creating a discrete probability mass function for each demographic strata.  Missing values are resolved through a weighted random draw using a fixed seed (`set.seed(42)`) for reproducibility: For each missing record, an age is sampled based on the probability weights of its specific category-gender strata.  If a strata lacks an observed distribution, the script applies a default uniform sampling range (Ages 19–54).

**VISA Status**
The script executes a hierarchical fallback protocol to synchronize student VISA Status markers between credential records and enrolment metadata. Attribute recovery is performed via a two-stage join to the credential_supvars_enrolment lookup table:

- Tier 1 (Specific): A six-column composite match (identifiers, institution, and program-specific metadata) to ensure program-level accuracy.
- Tier 2 (Broad): A four-column fallback (identifiers and school year) to capture status for records with inconsistent program descriptions.

The final status is assigned back to the main dataset, prioritizing Tier 1 over Tier 2 results. To resolve potential row duplication from many-to-many joins, ties are broken by choosing the maximum unique record ID.

**Delay Award Effect**
Delay award effect occurs when secondary credentials are awarded shortly after a student’s primary, highest-ranked degree. This procedure aligns longitudinal achievements with the most representative educational exit point.

A CONCATENATED_ID is generated to unify student records, prioritizing the ENCRYPTED_TRUE_PEN followed by a composite institutional key. The pipeline then isolates a baseline dataset containing only records flagged as a student's peak academic achievement (HIGHEST_CRED_BY_RANK == "Yes").

Valid secondary "delayed" awards occurring after the primary achievement are determined by category-specific thresholds:

- Permanent Retention: Apprenticeships, Bachelors, and Professional degrees.
- 36-Month Window: Advanced Diplomas and Certificates.
- 30-Month Window: Masters Degrees and Graduate/Post-Degree Diplomas.
- 18-Month Window: Associate Degrees and basic Certificates.

For students with qualifying secondary awards, the script selects the most recent occurrence to establish a final CREDENTIAL_AWARD_DATE. These values are integrated back into the main dataset ensuring that student records reflect either their original graduation date or their reconciled "delayed" exit point.

**Research University Flag adn Outcomes Credential Standardization**
The final processing phase applies institutional and categorical identifiers to standardize records for sector-level reporting. A binary flag, RESEARCH_UNIVERSITY, is assigned to records originating from research-intensive institutions (SFU, UBC, UNBC, UVic, RRU). PSI_CREDENTIAL_CATEGORY is standardized to ensure collapses disparate institutional titles into consistent categories required for provincial performance metrics.


#### Data Dictionary (Not Completed)

##### [Table 1 Name]


<ul class="list-variable-names">
<li>STQU_ID (Integer): A unique identifier for each student.</li>
<li>SURVEY_YEAR (Integer): The academic year in which the survey was administered.</li>
<li>SURVEY (String): A character code representing the survey (e.g. "BGS"). </li>
<li>INST_CD (String): A character code representing the institution in which a student was enrolled. </li>
</ul>

Key variables computed in this analysis:

<ul class="list-variable-names">
<li>Weight_NLS_CIP_NOC (Integer): RECALCULATED IN THIS ANALYSIS</li>
<li>Weight_OCC_CIP_NOC (Integer): RECALCULATED IN THIS ANALYSIS</li>
</ul>

 Variables not used in this analysis: 

<ul class="list-variable-names">
<li>AGE_AT_GRAD: NOT USED</li>
<li>Age_Grad_Group: NOT USED</li>
</ul>

### 01d-enrolment-analysis.R

#### Descriptive Methods

A dual-identifier strategy is used to leverage both the Encrypted Provincial Education Number (EPEN) and institutional student numbers and harmonize demographic attributes. 

Where a single student is associated with multiple conflicting gender or birthdate records across the longitudinal set, the first seen enrolment is used.

To standardize gender markers, values are first extracted from the STP Credential dataset to backfill enrolment records. For the remaining "Unknown" or missing entries, proportional imputation is performed; the observed frequency distribution of known gender labels within the first-time enrolment cohort is used as statistical weights to impute the missing population.

To address missing birthdates, a stratified proportional imputation based on gender-specific age distributions is employed. Following the assignment of a baseline age to a student's first enrolment record, subsequent enrolment records for the same student are updated through chronological increments (Current Year - Baseline Year), maintaining longitudinal consistency across the student's entire academic history.

#### Data Dictionary (Not Completed)

##### [Table 1 Name]

<ul class="list-variable-names">
<li>STQU_ID (Integer): A unique identifier for each student.</li>
<li>SURVEY_YEAR (Integer): The academic year in which the survey was administered.</li>
<li>SURVEY (String): A character code representing the survey (e.g. "BGS"). </li>
<li>INST_CD (String): A character code representing the institution in which a student was enrolled. </li>
</ul>

Key variables computed in this analysis:

<ul class="list-variable-names">
<li>Weight_NLS_CIP_NOC (Integer): RECALCULATED IN THIS ANALYSIS</li>
<li>Weight_OCC_CIP_NOC (Integer): RECALCULATED IN THIS ANALYSIS</li>
</ul>

 Variables not used in this analysis: 

<ul class="list-variable-names">
<li>AGE_AT_GRAD: NOT USED</li>
<li>Age_Grad_Group: NOT USED</li>
</ul>

## Key Output Tables

## Suggested Improvements

## Additional Details

_This template provides a comprehensive structure for documenting technical modules, supporting consistency and clarity across your project._